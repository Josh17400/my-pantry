import { HomeScreen } from '../features/home';

/**
 * Home route — product overview screen (M1 Track H).
 * Shell chrome lives in App; this page owns the overview body only.
 */
export function HomePage() {
  return <HomeScreen />;
}
